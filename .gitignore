_site
.sass-cache
.jekyll-cache
.jekyll-metadata
vendor
.unison.tmp-*
.DS_Store

# browser-tool debug artifacts (never commit)
.playwright-mcp/

# internal engineering docs — MUST NOT be in this public repo
docs/
byod-cr-explainer/

# generated cafe review HTML (build output of cafe/site/build-review.js) — keep the
# generator (build-review.js) + today-sheet-setup.md tracked; ignore only the output
cafe/site/index.html
cafe/site/today.html
cafe/site/review/
